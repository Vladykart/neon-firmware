# neon-firmware

Release channel for the M5StickS3 neon controller (https://github.com/Vladykart/neon-controller).
Devices poll `version.txt` and fetch `neon_controller.bin` from this repo (raw URL) when a newer version appears.
